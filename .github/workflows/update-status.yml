name: Check domains

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    inputs:
      ssl_threshold:
        description: "Days before SSL/WHOIS expiration to trigger an alert"
        required: false
        default: "29"
      response_time:
        description: "HTTP response time in ms to trigger an alert"
        required: false
        default: "1000"
  repository_dispatch:
    types: [self-trigger] 

permissions:
  contents: write
  issues: write

jobs:
  update-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        if: steps.domain_check.outputs.abort == 'false'
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt
        if: steps.domain_check.outputs.abort == 'false'

      - name: Generate config.json
        if: steps.domain_check.outputs.abort == 'false'
        run: |
          mkdir -p status
          REPO="${{ github.repository }}"
          BRANCH="${{ github.ref_name }}"
          USER="${REPO%%/*}"
          REPO_NAME="${REPO##*/}"
          cat <<EOF > status/config.json
          {
            "github_user": "$USER",
            "github_repo": "$REPO_NAME",
            "github_branch": "$BRANCH"
          }
          EOF
          echo "Generated status/config.json: "
          cat status/config.json

      - name: Check if domains exist
        id: domain_check
        run: |
          if [ ! -s domains.txt ]; then
            echo "No domains found. Aborting gracefully."
            echo "abort=true" >> $GITHUB_OUTPUT
          else
            echo "abort=false" >> $GITHUB_OUTPUT
          fi

      - name: Check domains
        if: steps.domain_check.outputs.abort == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          RESPONSE_THRESHOLD: ${{ github.event.inputs.response_time || '1000' }}
          DAYS_THRESHOLD: ${{ github.event.inputs.ssl_threshold || '29' }}
        run: python check_domains.py

      - name: Commit files
        if: steps.domain_check.outputs.abort == 'false'
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add status/*.json
          git add status/*.xml
          git add status/history/*.json
          git add status/history/*.xml

          if git diff --cached --quiet; then
            echo "no_changes=true" >> $GITHUB_OUTPUT
            echo "No changes to commit."
            exit 0
          else
            git commit -m "Data fetched for domains [skip ci]"
            echo "no_changes=false" >> $GITHUB_OUTPUT
          fi

          git push

      - name: Trigger self via repository_dispatch
        if: steps.domain_check.outputs.abort == 'false'
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          event-type: self-trigger
          client-payload: |
            {
              "ssl_threshold": "${{ github.event.inputs.ssl_threshold || '30' }}",
              "response_time": "${{ github.event.inputs.response_time || '1000' }}"
            }
